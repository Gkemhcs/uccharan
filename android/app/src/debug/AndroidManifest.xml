<?xml version="1.0" encoding="utf-8"?>
<!-- Merged only into debug builds. Adds the debug-only network security
     config that trusts user-installed CAs — see network_security_config_debug.xml
     for why. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config_debug" />
</manifest>
